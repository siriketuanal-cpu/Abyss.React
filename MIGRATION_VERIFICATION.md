# React移行テスト版 — 検証記録

## 実行済み検証

| 項目 | 結果 |
|---|---|
| TypeScript型検証 | 成功（`pnpm check`） |
| Vite本番ビルド | 成功（`pnpm build`） |
| ドットアビス回復サイクル維持 | 自動テスト成功 |
| 満タン到達時刻計算 | 自動テスト成功 |
| スターリープ4桁時刻入力 | 自動テスト成功 |
| ドットアビス1回／2回タップ | 参考表示・確定・タイマー開始を実画面確認 |
| ドットアビス長押し | 現在値インライン入力を実画面確認 |
| ドットアビス名称・ランク長押し | 名称保存、Lv.3→上限250への再計算を実画面確認 |
| 放置報酬 | 通常時長押しデイリー切替、1回タップ確認、確認中長押し受取を実画面確認 |
| Gジェネ2回タップ | 現在値インライン入力・保存を実画面確認 |
| Gジェネ長押し | 上限値インライン入力・保存を実画面確認 |
| Gジェネ名称・デイリー | 長押し名称編集、デイリー長押し切替を実画面確認 |
| スターリープ2回タップ | 討伐値・オーブ時刻入力・保存を実画面確認 |
| React専用保存 | 再読み込み後の値復元を実画面確認 |
| PWA | `manifest.json`、サービスワーカー登録、standalone設定を確認 |
| 保存復旧 | 直前バックアップからの復元と、初期化前の二段階確認を実画面確認 |
| 数値列の位置 | Gジェネとスターリープの主値・補助行が同一Y座標であることを実測確認 |
| v153再現レイアウト | ドットアビス440px・220.2pxグリッド、69.4px連結カード、ゲーム枠Y=294.2を実測確認 |
| v153再現操作 | ドットアビス参考中の通常タップ維持・長押し確定、放置報酬の別カード確認解除、GジェネARM維持、スターリープ2回タップ入力を実画面確認 |
| Gジェネ上限／オーブ | 500ms長押しの上限編集、オーブ2回タップ入力と保存を実画面確認 |
| 連結部・名称・確認待機 | スタミナ右端／放置報酬左端の罫線幅0px、名称長押し入力、参考表示・受取待機の暗転を実画面確認 |
| タッチ相当の追加確認 | 放置報酬確認待機ではスタミナ側にも`z-index:28`を付与し、連結ペアを一体で暗転前面に保持。`pointerType=touch`の500ms名称長押しで入力欄が開き、入力欄へフォーカスすることを確認 |
| 最終タッチ回帰 | 確認待機で放置報酬がARM、対になるスタミナカードが前面化、放置報酬左罫線幅0pxを確認。名称長押しは親カードの参考表示を開始せず、入力欄を開いてフォーカスすることを確認 |
| 対象限定の回帰修正 | 放置報酬確認待機は対象放置報酬だけを`z-index:28`で前面化し、スタミナ側は通常の暗転対象へ戻した。名称欄の通常タップはスタミナ参考表示を開き、500ms長押しは名称入力へ遷移することをタッチ相当で確認 |
| PWAキャッシュ調査 | 公開版では`abyss-timer-react-v1`が`/`と`/manifest.json`を恒久的にcache-firstで返し、サービスワーカーが制御中であることを確認。古いHTMLが残る原因のため、ナビゲーションをネットワーク優先へ変更する |
| PWA更新戦略 | 新しい`v2`サービスワーカーは`skipWaiting`・`clients.claim`・`updateViaCache:none`を使う。HTML、manifest、開発モジュールはnetwork-first、Viteのハッシュ付き`/assets/`だけはcache-firstとし、旧画面とHMR更新の取り違えを防ぐ。マニフェスト開始URLにも`?pwa=2`を付け、旧`/`キャッシュを使わずに更新版を起動できるようにする |
| PWA更新の実画面確認 | `/?pwa=2`でサービスワーカー制御中の起動、待機中ワーカーなし、manifestの開始URL反映、最新アプリシェルとエントリースクリプト取得を確認 |

## 保存設計

React版は`dotabyss:react:v1`を使う。既存`dotabyss:unified:v1`がある場合は初回読込時に互換形式として読み取る。保存前の直前状態は`dotabyss:react:backup:v1`へ保持し、将来の復旧UI追加に備える。

## 初回移行版の位置付け

React版は**既存HTML版と並行するv153同等化候補版**である。回復計算、長押し・インライン編集、名称・ランク編集、放置報酬の確認、保存復旧、PWA登録は移植済みである。主要レイアウトはv153の実測値へ揃えたが、最終的な実機Android Chromeでの指先操作感とサブピクセル描画は継続確認する。既存の安定版は保持する。
