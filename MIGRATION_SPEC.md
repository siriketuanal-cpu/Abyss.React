# 深淵タイマー React＋Vite 移行仕様

## 移行原則

このプロジェクトは既存PWAを置き換えるための**独立テスト版**である。v102、v138、v150、v153の既存HTMLは変更しない。React版が操作・保存・時間計算を再現できることを確認するまで、既存版を配布基準として扱う。

## ドメインモデル

| ドメイン | 状態 | 回復間隔 | 編集操作 |
|---|---|---:|---|
| ドットアビス・スタミナ | 現在値、上限、開始時刻、進行中状態 | 3分 | 1回タップで40基準の参考値、参考中の長押しで確定、通常時の長押しで現在値をインライン入力 |
| ドットアビス・放置報酬 | 開始時刻、上限時間、進行中状態、デイリー完了 | 12時間 | 1回タップで受取確認、確認中の長押しで受取、通常時の長押しでデイリー切替 |
| Gジェネ | 現在値、上限、開始時刻、進行中状態、デイリー完了 | 5分 | 1回タップでARM、2回タップで現在値入力、長押しで上限値入力 |
| スターリープ討伐 | 現在値、開始時刻、進行中状態 | 12分 | 2回タップで現在値をインライン入力 |
| スターリープオーブ | 現在値、開始時刻、進行中状態 | 6時間 | 2回タップで残り時間を4桁までの数字でインライン入力 |

## 重要な計算仕様

1. タイマー表示は分単位で切り上げる。
2. ドットアビスの現在値更新では、進行中の3分回復サイクルの残り時間を維持する。
3. 満タン後も到達時刻を保持して表示する。
4. 表示更新は単一の自己スケジュール型ループで、分境界へ同期する。
5. 進行中のタイマーがない場合は、更新ループを停止する。

## React構成

```text
client/src/
  components/
    AbyssPair.tsx
    GGenerationCard.tsx
    StarLeapCard.tsx
    TimerValue.tsx
  hooks/
    useTimerClock.ts
    useLongPress.ts
    usePersistentTimerState.ts
  lib/
    timerMath.ts
    storage.ts
    types.ts
  pages/
    Home.tsx
  index.css
```

## 保存・復旧方針

保存キーは既存版と衝突しないReact移行専用キーを使う。値を読み込む際は数値範囲・開始時刻・配列長を検証し、壊れた値は安全な既定値へ戻す。将来的な本移行に備え、既存形式を読み取ってReact専用形式へ変換する互換読込も実装する。

## 最初の移行範囲

初回では、全ゲームのタイマー表示、ドットアビスの回復サイクル維持、localStorage保存、各ゲームの現在値編集、Gジェネの上限編集、デイリーマークを移す。名称編集・ランク編集・放置報酬の受取確認は、同じイベント設計で移植するが、細かなピクセル調整は初回検証後に行う。
